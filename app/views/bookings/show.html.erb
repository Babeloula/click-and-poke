
<div class="container">
  <div>
    <div class="mt-5"><h2> Hi, <%= @booking.user.username %></h2>
    <h5>Your booking is <%= @booking.status %></h5></div>
      <div class="card-booking mt-5"><% if @booking.pokespot.photo.attached? %>
                <%= cl_image_tag @booking.pokespot.photo.key %>
                <% end %>
      </div>

        <p> <%= @booking.pokespot.name %></p>
        <p> <%= @booking.pokespot.description %></p>
        <div class="d-flex">
        <div class="mr-5"><p>Arrival: <%= @booking.start_date  %></div>
        <p>Departure: <%= @booking.end_date  %></div>
        <div><p>Amount: <%= @booking.pokespot.price %>₽</div>


        <p> <%= @booking.pokespot.pokemon_type %>
        <p> <%= @booking.pokespot.scarcity_drop_level %>
  </div>
</div>
